{
  "version": "0.0.0",
  "name": "am4games",
  "comment": "Dependencies must be duplicated from server/package.json (move to pre-deploy.sh?)",
  "engines": {
    "node": "11.0.0",
    "yarn": "1.x"
  },
  "dependencies": {
    "cookie-parser": "^1.4.3",
    "express": "^4.16.3",
    "mongoose": "5.3.9"
  },
  "scripts": {
    "": "for deploy on Heroku",
    "start": "node server/index.js",

    "": "for develop clients",
    "dev-user": "cd clients/user && yarn docker-dev",
    "dev-editor": "cd clients/editor && yarn docker-dev",
    "dev-admin": "cd clients/admin && yarn docker-dev",

    "": "for test clients",
    "test-user": "cd clients/user && yarn docker-test",
    "test-editor": "cd clients/editor && yarn docker-test",
    "test-admin": "cd clients/admin && yarn docker-dev",

    "": "for develop server",
    "dev-server": "cd server && yarn docker-dev",
    "dev-test": "cd server && yarn docker-test"
  }
}
