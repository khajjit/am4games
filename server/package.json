{
  "engines": {
    "node": "11.0.0",
    "yarn": "1.x"
  },
  "scripts": {
    "docker-dev": "sudo docker-compose up server_dev database",
    "docker-test": "sudo docker-compose up server_test database",
    "": "",
    "test": "echo \"tests not specified\"",
    "dev-start": "yarn install --check-files && sleep 5 && node database/seed.js && PORT=8080 nodemon index.js --watch"
  },
  "dependencies": {
    "cookie-parser": "^1.4.3",
    "express": "^4.16.3",
    "mongoose": "5.3.9"
  },
  "devDependencies": {
    "nodemon": "^1.17.5"
  }
}
