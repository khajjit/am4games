{
  "engines": {
    "node": "11.0.0",
    "yarn": "1.x"
  },
  "scripts": {
    "docker-dev": "sudo docker-compose up server_dev database",
    "docker-test": "sudo docker-compose up server_test database",
    "": "",
    "test": "echo \"tests not specified\"",
    "dev-start": "yarn install --check-files && sleep 5 && node database/seed.js && PORT=8080 NODE_ENV=development nodemon index.js --watch"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "cookie-parser": "^1.4.3",
    "express": "^4.16.3",
    "mongoose": "5.3.9",
    "multer": "^1.4.1"
  },
  "devDependencies": {
    "nodemon": "^1.17.5"
  }
}
