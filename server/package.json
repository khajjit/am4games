{
  "engines": {
    "node": "11.0.0",
    "yarn": "1.x"
  },
  "scripts": {
    "test": "echo \"tests not specified\"",
    "dev-start": "yarn install --check-files && PORT=8080 nodemon index.js --watch"
  },
  "dependencies": {
    "cookie-parser": "^1.4.3",
    "express": "^4.16.3",
    "mongoose": "5.3.9"
  },
  "devDependencies": {
    "nodemon": "^1.17.5"
  }
}
